# B07 checkpoint — saved UI message references

- Mapped packages: T06 and T17; broader packages remain in_progress.
- Branch: `codex/ui-context-mapping`; verified base `3b51647` (merged PR #14).
- Implementation commit / PR: this feature branch's implementation commit and PR.
- Status: **in_review** for the bounded backend message-map slice.
- Local implementation complete for this slice: yes. Deployed/enabled: no.
- Live extension integration and model-quality evaluation: pending.

Implemented capture schema 1.1 for an owned Gmail thread view, immutable visible
order and selection, authoritative cleaned excerpts, and version/age/budget checks.
A supported exact lookup returns one quoted saved message without inference; a
single-message summary sends only that source through the pinned native/Flow path.
Missing/cross-surface/ambiguous refs clarify; unsupported reference operations do
not execute a supported subset. New release wrapper leaves old assets unchanged.
The frontend contract, examples, diagram and rollback notes are in
[ui-context-mapping.md](../../ui-context-mapping.md).

| Assertion | Evidence in `backend/tests/test_ui_context.py` | Result |
|---|---|---|
| B07-A1 | API capture → request → worker → artifact; third visible is m2 while third chronological is m3; later sync/navigation cannot retarget | Pass, real PostgreSQL with synthetic UI |
| B07-A2 | Message/email vs thread/option, absent selection, multi-selection, out-of-range and multiple refs | Pass for supported message map; other map types clarify |
| B07-A3 | Foreign owner, other thread, deleted/duplicate/over-limit IDs, unknown surfaces, extra body/text keys, stale version/time | Pass; rejected before inference |
| B07-A4 | Original snapshot/hash/evidence unchanged after recapture, old snapshot ordinal behavior, pinned contracts, tampered route, concurrent request replay | Pass; full existing release regression also passes |
| B07-A5 | Screenshot-style “what's in the 3rd message?” returns m2's exact “Thanks for clarifying.” with one evidence source | Pass synthetically; real extension scenario pending |

Verification on Python 3.12 and a task-owned disposable PostgreSQL 16 container:

```bash
THREADLY_REQUIRE_TEST_DB=1 \
THREADLY_TEST_DB=postgresql+asyncpg://threadly:change-me@127.0.0.1:55444/threadly_test \
PYTHONPATH=backend python -m pytest backend/tests -q --tb=short
```

**318 passed, zero skipped**, including 43 new B07 cases and existing migration,
Flow runtime, draft and durable task tests. Initial focused run after fixing the
capture fixture/version contract: 39 passed; four additional tests passed in the
full run. The existing thread version begins at 0, and the new contract correctly
accepts it. Ruff app/tests, both documentation validators, generated card checks
and `git diff --check` pass. Hosted CI result belongs to the PR check suite.

Review covered single-statement owner/source/version reads, bounded memory/body
hydration, source injection as quoted data, release/prompt compatibility, retry
checkpoint validation, and cancellation fencing. No database transaction spans
inference. Source IDs never come from model output; scoped-summary validators reject
references to a second message. No new SDK dependency, AWS resources, model calls,
OAuth scopes or Google writes. Synthetic Flow execution is not a live AWS result.

No table/model change or new migration; Alembic head remains `e9b7120c4a63`.
Deploy API and worker together before enabling capture 1.1 in clients. Drain/cancel
new UI-release jobs before rolling back to an older worker. Existing snapshots and
artifacts require no backfill. Original schema 1.0 request hashes are unchanged.

Remaining: actual extension captures, arbitrary selected-text inputs, inbox-thread
maps, schedule-option maps, free-form reference Q&A and compound operations. Only
supported exact message forms are installed; this is not completion of all-intent
frontend integration. Client order is an assertion checked for owned IDs, not proof
of the screen's contents. Saved excerpts are partial and may be truncated.

Next: B08 durable typed clarification input using these frozen maps; B09 bounded
Q&A can reuse the binder. Browser integration owner: frontend, following the recipe
and navigation/multiple-chip cases in the runtime doc. Historical original-workspace
B00/B01 changes belong to separate work and were preserved. T06/T17 now link this
backend evidence without marking their broader acceptance criteria verified.
