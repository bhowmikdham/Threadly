# B07 — Bind UI selections and ordinal references

**Proposed implementation task. Generated from tasks.json; edit the manifest to change scope.**

Status: `planned`. Original packages: T06, T17.

Prerequisites: B00.

Owner: Backend. Reviewers: Backend, AI, Frontend.

## Objective

Resolve this/selected/third message against an immutable authorized view, never guessed backend chronology.

## Read first

- [Agent launch contract](../START-HERE.md)
- [Baseline and known traps](../BASELINE.md)
- [Quality gates](../QUALITY-GATES.md)
- [Shared design](../DESIGN.md): D5

Existing repository files (paths are relative to repository root):

- [backend/app/assistant/context.py](../../../backend/app/assistant/context.py)
- [backend/app/assistant/routing.py](../../../backend/app/assistant/routing.py)
- [backend/app/schemas/assistant.py](../../../backend/app/schemas/assistant.py)
- [backend/app/api/routes/assistant.py](../../../backend/app/api/routes/assistant.py)
- [backend/app/db/repositories.py](../../../backend/app/db/repositories.py)
- [frontend/README.md](../../../frontend/README.md)

Proposed implementation locations (may not exist yet; follow current repository conventions):

- `backend/app/assistant/ui_context.py`

## Implementation sequence

1. Define strict capture contract for surface kind, visible-order IDs, selected IDs, capture time and version. Validate every reference against authenticated ownership and bounded supported scope.
2. Persist the UI mapping independently from backend chronology, hydrating authoritative text from synced records. Label arbitrary selected text as user input, not provider evidence.
3. Extend routing/reference binder with exact map semantics and typed ambiguity outcomes. Missing, changed or cross-surface references request clarification.
4. Add versioned context schema/manifest policy without altering old snapshots. Preserve thread excerpt coverage/limits and avoid unnecessarily collecting full screen contents.

## Acceptance assertions

Implement independent tests for these behaviors. Record each assertion in the checkpoint;
do not mark a live assertion passed using only a mock. Apply all relevant QUALITY-GATES checks.

- **B07-A1:** Third message resolves to the captured visible third entry even when current inbox/thread order changes.
- **B07-A2:** Third thread versus third message versus third schedule option have distinct maps; absent/ambiguous map clarifies.
- **B07-A3:** Cross-owner, deleted, duplicated, over-limit or unsupported IDs fail before inference; client body spoofing cannot replace backend text.
- **B07-A4:** Later navigation/reordering cannot mutate existing evidence; old snapshots still read and old queued releases retain behavior.
- **B07-A5:** Follow-up examples from the user's screenshots return exact selected-message evidence, not a whole-thread summary.

## External integration gate

Actual extension selection map integration requires frontend code; synthetic maps can verify backend semantics first.

## Finish and hand off

Owned context/ref maps available to B08/B09 and frontend reference rendering.

Run focused tests and settled full regression when changing runtime code. Run schema/migration
checks when changing storage; pin/evaluate prompt or Flow changes. Use the existing error
envelope and owner checks. Do not deploy or enable real writes from this planning document.

Save evidence to `docs/backend-execution/checkpoints/B07.md` using
[the checkpoint template](../CHECKPOINT.md). Update this manifest status and the original
T-task evidence only as justified. Commit a reviewable PR; no auto-merge. Report next eligible
task and any remaining live gate. An entire mapped T package may still be incomplete.
