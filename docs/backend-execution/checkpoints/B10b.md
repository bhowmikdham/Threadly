# B10b checkpoint — captured-text lookup → draft

- B10 remains **in_progress**. This slice closes the installed captured-lookup pair
  portion of A1/A5, not the complete natural-language planner or Calendar graph.
- Base: PR #29 merge `e93541835e4e651a291a4a4d3dbf00839d4ab0c8`.
- Branch: `codex/lookup-draft-workflows`. Commit/PR: containing review branch.
- Scope: strict explicit `lookup_then_reply`/`lookup_then_compose`, same task service,
  worker, lease fences, two step rows and immutable artifact streams.
- Source scope: selected saved thread capture only; mailbox search/planning separate.
- No migration (head `a0426e9bc731`), dependency, Google scope or AWS resource change.
- No deployment, live model/Google calls, send, booking or frontend integration.

## Delivered / mapped assertions

| Assertion | Evidence |
|---|---|
| B10-A1 | Native lookup checkpoint survives draft failure; retry keeps ID/attempt 1 and produces one final draft |
| B10-A2 | Original summary contract hash unchanged; existing compound/draft history tests retained; no schema or historical identity change |
| B10-A3 | Cancel, source change and lease replacement during draft fence late publication; shared executor retains existing both-stage races |
| B10-A4 | Changed source stops; checkpoint/dependency corruption stops; historical artifacts remain; source replacement still requires a new request |
| B10-A5 | Both explicit pairs pass route → service → PostgreSQL → worker; unsupported templates/keys rejected before steps; no/too-many matches stop before model |
| B10-A6–A8 | Broader natural-language graph, clause/negation completeness and Calendar remain unimplemented; no partial-template fallback added |

The new `lookup-draft-template-1.0.0` policy/request schema has a separate pinned
fixture and preserves `compound-template-1.0.0`. Native/Flow generation remains
selected by the saved registry. Actual FlowInvoker with fake SDK verifies only the
draft calls the pinned Flow after configuration changes; native lookup needs no AWS.
Prompt-source injection is tested as an authority boundary, not a semantic guarantee.

## Validation

- Python 3.12, isolated PostgreSQL 16 `threadly-command-plan-test-db`, port 55449.
- Initial focused run: **39 passed, 1 failed**. The worker correctly detected a changed
  dependency hash; adjusted the test's error expectation, without weakening rejection.
- Expanded focused lookup + original compound suite: **43 passed, zero skips**.
- Full backend suite: **710 passed, zero skips**, 184.11 seconds, including migrations
  and existing email recovery regressions. Two existing dependency deprecation warnings.
- Ruff app/tests, whitespace, generated handoff cards and both documentation validators:
  **passed**. Mermaid source only; rendering not performed.
- Remote exact-head CI: tracked on the containing PR after publication.
- Live Haiku, AWS resource/provider and frontend quality gates: **not run**.

## Focused review and rollout

Reviewed explicit owner/recipient binding, literal native query, matching-message plus
reply-target source selection, artifact-local source numbering, exact IDs/evidence,
no DB transactions over inference, stale/cancelled lease publication, immutable lookup
reuse, and original request/release compatibility. No generic graph executor added.
New API and worker must deploy together. Rollback to old code cannot read/run stored
lookup templates; drain tasks and retain compatible history readers.

## Next work

Finish versioned full-command planning with explicit clause/negation coverage and
typed clarification, plus broader lookup coordination. Keep summary/slots/reply
unavailable until B12/B13 are implemented. Public Gmail approval/send remains gated
on controlled live email evidence. See [whole-backend report](../../backend-progress.md)
and [runtime/API details](../../lookup-draft-workflows.md).
