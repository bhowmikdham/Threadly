# B00 — Verify baseline and settle shared contracts

**Proposed implementation task. Generated from tasks.json; edit the manifest to change scope.**

Status: `planned`. Original packages: T01, T02, T04, T05, T08, T10, T11, T13.

Prerequisites: None beyond verified baseline access.

Owner: Backend. Reviewers: Backend, AI, Frontend.

## Objective

Prepare a reproducible implementation baseline; preserve merged behavior and record the exact integration targets.

## Read first

- [Agent launch contract](../START-HERE.md)
- [Baseline and known traps](../BASELINE.md)
- [Quality gates](../QUALITY-GATES.md)
- [Shared design](../DESIGN.md): D1, D2

Existing repository files (paths are relative to repository root):

- [AGENTS.md](../../../AGENTS.md)
- [docs/api-contract.md](../../../docs/api-contract.md)
- [docs/data-model.md](../../../docs/data-model.md)
- [docs/architecture.md](../../../docs/architecture.md)
- [backend/tests/conftest.py](../../../backend/tests/conftest.py)
- [backend/app/db/models.py](../../../backend/app/db/models.py)
- [frontend/README.md](../../../frontend/README.md)

Proposed implementation locations (may not exist yet; follow current repository conventions):

- `docs/backend-execution/checkpoints/B00.md`

## Implementation sequence

1. Verify PR #9 ancestry, current integration branch, clean/dirty tree and Alembic head. Locate actual frontend code from its README/remote branches without merging it blindly.
2. Read current APIs and action/slot/route planning schemas; record proposed API naming, sender scope, default-disabled writes and legacy /draft mapping policy. Keep legacy send stub until safe delegation exists.
3. Set up Python 3.12+ and backend dev dependencies in an isolated environment. Start an isolated PostgreSQL 16 test DB and run baseline checks; record exact commands/environment.
4. Create the first committed checkpoint and a concrete migration/PR sequence. No new production endpoint is needed in this task.

## Acceptance assertions

Implement independent tests for these behaviors. Record each assertion in the checkpoint;
do not mark a live assertion passed using only a mock. Apply all relevant QUALITY-GATES checks.

- **B00-A1:** Run current full backend tests with THREADLY_REQUIRE_TEST_DB=1; investigate any regression from historical 233 baseline, do not force a count.
- **B00-A2:** Run Ruff and both documentation validators; confirm all referenced code paths exist and migration graph has one current head.
- **B00-A3:** Verify known examples still route summary/reply/compose and leave unavailable capabilities explicit; use existing tests, no live provider call needed.

## External integration gate

Frontend branch and live deployment state must be discovered, not assumed. Missing cloud credentials do not block local baseline work.

## Finish and hand off

B01 can implement capabilities; B02 can implement action storage against the recorded contract. T packages remain partial until their wider criteria pass.

Run focused tests and settled full regression when changing runtime code. Run schema/migration
checks when changing storage; pin/evaluate prompt or Flow changes. Use the existing error
envelope and owner checks. Do not deploy or enable real writes from this planning document.

Save evidence to `docs/backend-execution/checkpoints/B00.md` using
[the checkpoint template](../CHECKPOINT.md). Update this manifest status and the original
T-task evidence only as justified. Commit a reviewable PR; no auto-merge. Report next eligible
task and any remaining live gate. An entire mapped T package may still be incomplete.
