# B16 checkpoint — generation registry subset

- Branch: `codex/bedrock-workflow-runtime`; verified merged base `4555b04` (PR #13).
- Status: **in_progress**, not full B16/B17 completion; see PR for implementation commit.
- Local subset: registry + existing generation worker integration implemented.
- Live integration/deployment: pending; user reported six prototypes Prepared,
  but those prototype envelopes differ from this runtime contract.

Behavior: optional typed registry pins three installed operations to native handlers
or numbered Bedrock Flow targets; task acceptance saves the registry and contract
identity. Existing empty-config admission, artifact schemas, draft envelopes and
fenced publication remain. Changed active registry affects new tasks only. Unknown
operations/graphs/models/accounts/budgets/test aliases fail closed. `/readyz` checks
configuration and authenticated `/assistant/workflows` describes installed paths.

B16-A1/A2/A3/A4: local compatibility, saved-target, invalid-configuration and native/
Flow artifact replay tests pass. Full historical provider-setting migration is not
implemented: old manifests contain hashes, and changed model/router configuration
fails `release_unavailable` rather than reconstructing historical settings.
B16-A5: malformed local configuration fails closed; actual AWS availability is
checked on invocation, not asserted by local readiness.

Evidence: full backend run with required disposable PostgreSQL: **273 passed, zero
skips**. After adding the synthetic evaluation harness, focused workflow tests:
**41 passed, zero skips**. Ruff app/tests, generated AWS template cfn-lint, both plan/
handoff validators and diff whitespace checks pass. Commands and setup are in
`docs/workflow-runtime.md`; DB used a task-owned PostgreSQL 16 container at local
port 55443. No live AWS/Google invocation or deployed application test performed.

Review: identity/source/envelope remains server-bound. No new tables or migrations.
Registry rollback does not reinterpret queued jobs. Changing provider settings can
invalidate historical jobs; retain/drain old configuration before such a change.
Alias race cannot be eliminated by client checks: restrict deployment mutations;
pre/post checks reject detected movement. No arbitrary tool grants or source URLs.

Next: review this bounded runtime subset, evaluate and publish compatible AWS
versions, then integrate B09/B10/B11/B12/B13 operations as their implementations land.
B16 and B17 remain in_progress; original T packages are not marked verified.
