# B06 — Reconcile uncertain Gmail sends and complete email gate

**Proposed implementation task. Generated from tasks.json; edit the manifest to change scope.**

Status: `in_review`. Original packages: T12.

Prerequisites: B05.

Owner: Backend. Reviewers: Backend, Security/release owner.

## Objective

Recover ambiguous sends using provider evidence while refusing duplicate sends from inconclusive results.

## Read first

- [Agent launch contract](../START-HERE.md)
- [Baseline and known traps](../BASELINE.md)
- [Quality gates](../QUALITY-GATES.md)
- [Shared design](../DESIGN.md): D3, D4

Existing repository files (paths are relative to repository root):

- [backend/app/sync/gmail.py](../../../backend/app/sync/gmail.py)
- [backend/app/db/models.py](../../../backend/app/db/models.py)
- [backend/tests/test_draft_workflows.py](../../../backend/tests/test_draft_workflows.py)
- [docs/assistant-draft-review.md](../../../docs/assistant-draft-review.md)

Proposed implementation locations (may not exist yet; follow current repository conventions):

- `backend/app/actions/gmail_reconciliation.py`
- `docs/email-actions.md`

## Implementation sequence

1. Implement bounded, paginated Sent candidate lookup by persisted Message-ID and time/account scope, followed by matching headers/content/thread/recipients as observable.
2. Document provider-normalized MIME comparison, Bcc visibility limits and confidence criteria. Empty immediate search, mismatched/multiple candidates or lost read scope remain unknown.
3. Persist reconciliation observations/due times/attempt limits; expose outcome_unknown and manual inspection guidance. Reconciliation is read-only; never auto-resend merely due timeout/budget exhaustion.
4. Handle late worker success as evidence without bypassing fencing, and definitive non-execution separately from unproven absence.
5. Integrate legacy send only if it maps to the same owned action/approval path; otherwise retain 501. Complete email runbook and live test-account checklist.

## Acceptance assertions

Implement independent tests for these behaviors. Record each assertion in the checkpoint;
do not mark a live assertion passed using only a mock. Apply all relevant QUALITY-GATES checks.

- **B06-A1:** Simulated accepted send with lost response reconciles to one known Gmail ID and never issues a second POST.
- **B06-A2:** Delayed indexing, empty pages, duplicate/mismatched Message-ID candidates, wrong account and incomplete Bcc evidence stay unknown.
- **B06-A3:** Repeated reconciliation, process restart and late responses are idempotent and auditable; unknown work is not exhausted into a fresh send.
- **B06-A4:** Definite rejection follows tested transition policy; manual retry uses a new explicit action only when justified and authorized.
- **B06-A5:** One controlled end-to-end new email and reply demonstrate threading/recipient behavior and result IDs before write flag enablement.

## External integration gate

The last case requires explicit authorization, Google test accounts and verified scopes. Report simulated pass and live pending separately.

## Finish and hand off

Email lifecycle is ready for integration once live gate passes. B15 may reuse action infrastructure with Calendar-specific reconciliation.

Run focused tests and settled full regression when changing runtime code. Run schema/migration
checks when changing storage; pin/evaluate prompt or Flow changes. Use the existing error
envelope and owner checks. Do not deploy or enable real writes from this planning document.

Save evidence to `docs/backend-execution/checkpoints/B06.md` using
[the checkpoint template](../CHECKPOINT.md). Update this manifest status and the original
T-task evidence only as justified. Commit a reviewable PR; no auto-merge. Report next eligible
task and any remaining live gate. An entire mapped T package may still be incomplete.
