# B14 checkpoint — meeting offers and typed assistant scheduling

- B14 / T06, T11, T16: **in_progress**. This is the offer/selection persistence
  slice; the whole scheduling conversation package is not complete.
- Branch `codex/calendar-negotiation-state`; verified PR #33 merge base
  `7a59e31baf0c8c2a27b2219f15bcde3ba730dfcf` on `codex/assistant-intent-routing`.
  Commit/PR: containing review branch; attach exact-head CI to that PR.
- PR #33 CI is now verified: implementation `8aa276322f1a71889a1b9fe4c4e58cf926987c0a`
  passed 878 backend tests, 12 EC2 checks, 19 summary-console checks, lint and both
  Flow template checks in run 35077521850. The prior capacity-blocked status read
  was not a failing code check.
- Migration `e14026e9a034` follows `d13026e9a033`.
- No frontend, live Google/model/AWS call, cloud configuration, deployment, email
  send, event creation or write enablement. Original dirty auth/ML checkout preserved.

## Bounded scope and prerequisites

B13's deterministic slot read API is merged. B10's four reviewed compound kernels
exist, but automatic/broader Calendar graphs do not; B14a does not pretend that
broader prerequisite is complete. Install the owned negotiation state independently
before adding assistant scheduling orchestration in B14b. B12/B13 live gates remain.

Create a thread-bound negotiation; adopt a fresh nonempty owned slot query into an
immutable offer revision; explicitly select a stored option; re-read exact availability;
close or replace offers while preserving historical choices. Expose current usability
and blockers separately from historical state. Selection never approves booking.

The persisted selection receipt precedes Google reads. Account → preferences → thread
→ negotiation locks fence publication; no caller DB transaction spans reads. Concurrent
duplicates share one check. New email/preferences, expiry, replacement or close prevent
late selection publication. Sync records actual source update time, so transaction-start
clock values cannot let an older query pass a source-change adoption check.

Contract, API examples, state/sequence diagrams and recovery rules:
[meeting negotiations](../../meeting-negotiations.md).

## Acceptance mapping — do not mark all B14 assertions passed

| Assertion | Evidence in B14a | Still required |
|---|---|---|
| B14-A1 | B13 handles contextual time/clarification/fewer slots; B14a adopts its exact stored options and handles unknown/conflict on recheck | End-to-end assistant extraction, B08 scheduling clarification, artifacts and generated reply in B14b |
| B14-A2 | Typed selection rejects extra times/prose/attendee-availability/approval fields, foreign/unknown slot IDs and unrelated recheck payloads | Pinned scheduling model schemas/prompts plus adversarial replay/live evaluation |
| B14-A3 | Expired, superseded, wrong-owner/wrong-negotiation choices cannot become usable; no event action is created | B15 independently enforces exact event approval/dispatch guards |
| B14-A4 | Competing offer edits, duplicate selection, new thread/preferences during read, close/replacement during check, source timestamp and rollback cases | B14b task/artifact/draft edit integration and real staging interaction |
| B14-A5 | Stored offer/explicit selection creates no action/approval/event; mail text has no endpoint to authorize selection/booking | Sending slot offers through B03–B06; later-email interpretation and user confirmation in B14b; booking B15 |

## Verification

Python 3.12 and the isolated PostgreSQL 16 container `threadly-slots-test-db` on
127.0.0.1:55452, retained from this task's B13 run and verified before reuse. No live
mailbox DSN. Google is a fake httpx transport; fixture responses are not live evidence.

Observed checks:

- First meeting API/concurrency suite: **19 passed, zero skips**.
- New migration + existing broad/slot migrations: **3 passed, zero skips**. New test
  also drives API → service → actual migrated PostgreSQL → fake Google → publication.
- Expanded meeting/migration suite: **24 passed, zero skips**.
- Source-clock/recheck-binding + meeting/migration/sync suite: **28 passed, zero skips**.
- Final review added competing offer-edit and publication-rollback regressions and
  disabled offer usability while a selection check is in progress. Final checks below.
- Full local regression before the last two cases: **904 passed, zero skips**,
  320.35 seconds. Final affected meeting/slot-service/migration/sync run: **53 passed,
  zero skips**, 48.51 seconds. Two existing dependency deprecation warnings.
- Ruff app/tests/new migration, whitespace, both documentation validators, generated
  cards and updated runtime links/fences: **passed**. Mermaid sources were not rendered.
  Exact-head GitHub CI will include all additional cases; attach its result to the PR.
- Actual migration tests cover old slot data, empty install, schema drift, owner and
  same-negotiation pointer/FK enforcement, immutable offers/terminal selections,
  exact version advancement, closure, guarded downgrade and legal empty rollback.
- Final publishing review separated local close from provider permissions; the owner
  can close/replay after scope revocation or disconnection. Selection remains gated.
  Two additional no-network/owner-isolation regressions cover this recovery path.
  Final meeting + migrated-API suite: **30 passed, zero skips**, 31.26 seconds.
- No prompt/model/Flow changes or quality evaluation; no live provider claim.

Reproduce from `backend/`:

```bash
THREADLY_REQUIRE_TEST_DB=1 \
THREADLY_TEST_DB=postgresql+asyncpg://threadly:change-me@127.0.0.1:55452/threadly_test \
python -m pytest -q --tb=short
python -m ruff check app tests alembic/versions/e14026e9a034_meeting_negotiations.py
```

## Focused review and rollout

Reviewed owner/same-negotiation relationships, exact query/slot binding, source
version and actual-update timestamps, lock order, no network in transactions,
duplicate reservations, races, immutable history, stale GET/replay, no implicit
approval and rollback/data retention. Existing assistant task releases/hashes and
email action gates are preserved. Check receipts cannot silently switch slots.

Apply the migration with matching API/workers under normal backup/deploy sequencing.
No new environment variable, AWS resource or Google write scope. No deployment is
claimed. Downgrade refuses with negotiation rows. Referential retention and cleanup
must be coordinated in B19; expiry never means automatic physical deletion.

A killed process may leave checking until a caller explicitly replaces the offer or
closes the negotiation. There is no automatic check job/retry in this slice. Incoming
unsynced email and remote Calendar updates remain outside the local source snapshot.
Inspect usability and refresh before any later booking; no exclusivity is promised.

## Next work / total remaining

**Historical B14a handoff (superseded by B14b1 below):** validated scheduling constraints,
request-vs-message anchors, B08 task continuation, schedule_options/availability
artifacts, grounded optional drafts, and later-email option-selection proposals that
require explicit user confirmation. Preserve the stored offer revision/slot mapping;
never treat email “yes” as event approval. Pin and evaluate new prompts/releases.
Then B15 handles exact event preview/approval/dispatch/reconciliation.

After B14a, 21 initial-release packages have 4 verified, 11 partial/in-progress/in-review
and 6 planned entries: **17 still open**, not 17 equally sized new implementations.
The five B21–B25 later extensions make 22 open packages across the full 26-package
roadmap. Full breakdown and separate deployment truth: [backend progress](../../backend-progress.md).


## B14b1 — typed assistant scheduling tasks (current PR)

Status remains **in_progress** for B14/T06/T11/T16. Branch
`codex/assistant-scheduling`, based on verified PR #34 merge
`8712a92c76527852aede9a6d6f4c498e5d663e02`. PR #34 final implementation
`f4875bd76dc55a95aa080156bc57129d0b5aee48` passed CI run 35181338415:
908 backend tests, 12 EC2 checks, 19 summary-console checks, lint and Flow validations.
The current implementation commit and final CI receipt belong to the containing PR.

### Behavior and boundaries

- `POST /assistant/scheduling-requests` saves explicit check_time/suggest_slots,
  typed constraints, actual Calendar grants/preferences and owned optional source.
- Request-vs-message date anchor is chosen explicitly and persists across answers,
  worker restarts and day boundaries. Contextual AM/PM and DST handling reuse B13.
- Existing task/job/question/input/artifact/event state now supports a pinned typed
  scheduling release. The new `/scheduling-inputs` schema preserves older releases.
- Current task/lease/source/preferences/receipt fences guard publication. Completed
  B13 reads are reused after publication rollback. Processing/expired receipts need
  an explicit new task; no silent requery or changed date/time is substituted.
- Artifacts preserve exact stored options, unknown/fewer/elapsed outcomes and date
  provenance. GET supplies current usability/blockers separately from history.
- Fresh nonempty query IDs can be explicitly adopted by B14a; API test covers it.
  No automatic offer mutation, generated reply, send, approval or event creation.
- Migration `f14026e9a035` adds immutable nullable scheduling input and a contract
  check. Prior tasks/hashes remain intact. Downgrade refuses while new tasks exist.
- No model/prompt/Flow change, new dependency, environment variable or cloud change.
  Live model and Google evaluation is not claimed. Original dirty checkout untouched.

### Acceptance mapping

B14-A1 now covers the **typed** tomorrow-at-four/three-options path, partial input,
contextual disambiguation, unknown/fewer slots and persisted artifacts. Generated
reply and natural-language extraction remain. A2 now rejects caller-provided slot
IDs/write fields and unrelated query substitution; model-output evaluation remains.
A3/A4 retain B14a guards plus task cancellation/source change/publication rollback,
lease replay and answer races. A5 still has no automatic external action; receiving
an email remains outside this handler until an evaluated proposal path is installed.

This is a coherent first scheduling-task integration, not all of B14b. B08's shared
state and ownership machinery are reused, but historical `/inputs` contracts are
not expanded with scheduling-only fields. Main `/requests` natural-language routing
and compound Calendar graphs remain unavailable rather than dropping sub-intents.

### Actual checks and local environment limitation

- Initial new suite: 21 passed, 2 failed; fixed async response loading after answer
  acceptance. Expanded new scheduling + migration + broad migration: **25 passed**.
- Expanded scheduling/recovery + actual migrated-API suite: **32 passed, zero skips**.
- One additional fixed-anchor/DST mapping case was added before full regression.
- Full local regression: **926 passed, 15 failed, zero skips**. Failures cluster in
  existing expiry/retry tests. A direct query measured the disposable PostgreSQL
  clock **16.046 seconds behind host time**. Two representative failures (old worker
  lease expiry and slot expiry after a lock wait) reproduce on unchanged PR #34 code.
  This local full run is not reported as passing; final full CI is required.
- New DB-clock expiry fixtures use the database's clock for DB deadlines; existing
  expiry tests/assertions are unchanged. No test is skipped or weakened to hide this.
- Migration tests install from prior head, check schema drift, preserve older task
  data, enforce input immutability/intent checks and guarded/empty rollback. They
  drive API → question → typed answer → worker → fake Google → artifact on actual
  Alembic tables. Broad existing migration test also advances to the new head.
- Final scheduling + new migration + broad migration run: **34 passed, zero skips**,
  38.00 seconds. Ruff, whitespace, both documentation validators, generated cards
  and updated runtime links/fences passed. Final exact-revision CI is recorded
  in the PR before handoff. Two existing dependency deprecation warnings.
  Mermaid source diagrams are provided; no rendered diagram QA claimed.

Use Python 3.12 and an isolated PostgreSQL 16 instance (local fixture container
`threadly-slots-test-db`, 127.0.0.1:55452). From backend/:

```bash
THREADLY_REQUIRE_TEST_DB=1 \
THREADLY_TEST_DB=postgresql+asyncpg://threadly:change-me@127.0.0.1:55452/threadly_test \
python -m pytest tests/test_assistant_scheduling.py tests/test_scheduling_migration.py -q
python -m ruff check app tests alembic/versions/f14026e9a035_assistant_scheduling.py
```

### Review, rollout and remaining work

Reviewed lock order (account/preferences/source before task), network boundaries,
owner/version/lease fences, accepted input immutability, duplicate answers, exact
query binding, empty/unknown outcomes, historical usability and old request/release
compatibility. Apply the migration with matching API/worker versions; no deployment
or EC2 state change is claimed. Clock alignment remains an operational test-host
requirement; no host/VM clock settings were changed.

**Next B14b2:** pinned and evaluated natural-language scheduling extraction with
explicit constraint review into this handler; slot-grounded generated replies;
later-email option interpretation as a user-confirmed proposal. Then B15 implements
exact approved event execution and uncertain-result reconciliation. The initial
ledger remains 4 verified / 11 partial / 6 planned, because B14 is still incomplete.

Runtime examples, limits, diagrams and failure semantics:
[assistant scheduling](../../assistant-scheduling.md). Whole remaining work:
[backend progress](../../backend-progress.md).
