# B17 checkpoint — single-operation Flow invocation subset

- Branch/base: `codex/bedrock-workflow-runtime` / merged `4555b04`.
- Status: **in_progress**; local single-operation subset implemented.
- Shared test evidence: [B16 checkpoint](B16.md).
- Runtime/deployment guide and all remaining gates: [workflow runtime](../../workflow-runtime.md).

Implemented: lazy off-event-loop InvokeFlow with input/output/event/time budgets,
strict output/completion processing, sanitized typed errors, client/stream cleanup,
pre/post target checks, persisted target provenance, existing artifact validation,
cancellation/lease fencing, runtime Flow/role template rendering, candidate registry/
worker IAM generation, and an explicitly billed three-case synthetic smoke harness.

B17-A1: locally tests output-then-failure, missing/duplicate completion/output,
access/throttle errors, cancellation and input-required rejection. This graph does
not support resumable AWS multi-turn; those events fail explicitly until B08.
B17-A2: **callback grant assertions pending**. This release uses owner-bound context
hydrated by the existing backend before invocation and has no public read callback.
It neither implements nor claims the planned grant/nonce read-bridge protocol.
B17-A3: exact allowed graph, cloud masking, source-ID/output validation, envelope
exclusion and sanitized error tests pass; live adversarial language eval pending.
B17-A4: summary/reply/compose traverse task → fake SDK → real PostgreSQL artifact,
with source/envelope validation and cancellation fencing. B10 compound streams and
other/plan/schedule handlers remain pending.
B17-A5: **pending**. Actual application-compatible Flow resources and live model
results have not been created/verified by this change. User-reported prototype
preparation is not evidence for this runtime envelope.

Focused second review corrected the AWS event node type to `FlowOutputNode`, ignored
changing request metadata when comparing aliases, rejected test aliases locally,
and ensured cancellation prevents dispatch after background preflight. Existing
summary/draft contracts remain the final authority after model generation.

No production deployment, IAM mutation, external action or schema migration performed.
The renderer and evaluation command are prepared for the authorized live release
step. Retain native routing, older aliases and queued jobs during staged promotion.
B08/B09/B10 and Calendar/action dependencies remain necessary for a complete product.
