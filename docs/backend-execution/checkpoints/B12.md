# B12 checkpoint — Calendar read foundations

- Task B12 / T03, T15: **in_review**. Local implementation complete; live Google gate pending.
- Branch `codex/calendar-read-foundations`; verified PR #31 merge base
  `aeefd320051fdeca4c02b8f3c1cacda8f78df8b1`. Commit/PR: containing review branch.
- B01 code is merged; its live OAuth gate remains open. Calendar fixtures permit
  local progress without claiming that broader prerequisite is live verified.
- New migration `c12026e9a032` after `b10c026e9a31`.
- No live Google/AWS/model calls, cloud configuration, deployment, write enablement
  or frontend edits. Original auth/classifier checkout remains untouched.

## Implemented behavior

Narrow Calendar read consent via authenticated account-bound reconnect; actual list
and freebusy grants required. Owned current-ACL listing, explicit user preferences
with expected-version saves, and bounded freebusy evidence with per-calendar unknown
coverage. Google reads hold no database transactions. Fresh account/preference locks
fence publication and evidence reuse. Read grants/ACLs cannot enable event writes.

Storage has one owner preference row and immutable expiring evidence; no event body,
title or token is persisted in evidence. Old jobs/releases, action approval gates
and unsupported Calendar compound behavior remain unchanged. Fixed limits and complete
API examples: [Calendar read contract](../../calendar-reads.md).

## Acceptance evidence

| Assertion | Local evidence | Live boundary |
|---|---|---|
| B12-A1 | Real API/DB two-user isolation; partial/unknown/revoked scopes; unknown calendar selection; reader ACL accepted only for reads, write readiness stays false | Actual Google consent/ACL smoke pending |
| B12-A2 | Concurrent initial saves and updates yield one winner/one conflict; database trigger requires advancing version | No live dependency for local version semantics |
| B12-A3 | Error/omitted/malformed calendars remain unknown alongside known empty calendars; removed list entry is not silently dropped | Live freebusy response comparison pending |
| B12-A4 | Naive/reversed/oversized windows, invalid timezone/working policy, calendar/page/interval/response-size limits, duplicate IDs and pagination cycles rejected | DST slot arithmetic belongs to B13 |
| B12-A5 | Mock HTTP observes zero idle caller transactions; preferences/disconnect/account changes during fetch prevent evidence; rollback leaves no record | Actual latency/remote changes not measured |

Tests: `test_calendar_client.py`, `test_calendar_service.py`,
`test_calendar_migration.py` plus existing auth/migration/full regressions.
Python 3.12; disposable PostgreSQL 16 `threadly-calendar-test-db` on 55451.

Observed checks:

- Initial focused run: 48 passed, 9 failed from duplicate `transport` kwargs in the
  test wrapper. Fixed the fixture injection; implementation assertions unchanged.
- Focused Calendar + Google capabilities + real migrations: **59 passed, zero skips**.
- Expanded focused suite: **64 passed, zero skips**.
- First full run: **819 passed, 1 failed**. The concurrency fixture's global idle-
  transaction observer counted another request's legitimate pre-fetch transaction.
  Reproduced the failure, moved the assertion to a deterministic barrier with both
  HTTP calls waiting, and retained the zero-open-transaction assertion there.
- Settled full suite: **820 passed, zero skips**, 217.96 seconds; two existing
  dependency deprecation warnings.
- Final review added the `tzdata` runtime fallback (2026.4 installed locally) and
  isolated subprocess test with `PYTHONTZPATH=""`. Affected transport/schema/fallback
  suite: **41 passed, zero skips**. Exact-head CI includes this additional test.
- Final B13 seam review added a bounded 245-minute lookback so after-event buffers
  can account for recently ended meetings, with acceptance/rejection regression cases.
  Final affected Calendar/auth/migration suite: **67 passed, zero skips**, 19.61 seconds.
  Exact-head CI covers all 823 backend cases after these three additional tests.
- Ruff app/tests/new migration, whitespace, both documentation validators, generated
  cards, and updated runtime-document links/fences: **passed**.
- Real Alembic coverage: empty install, old-task preservation, drift, FK/check/trigger
  assertions, legal rollback and refusing destructive rollback with user data.
- No model/prompt/Flow change, so no new model-quality claim or paid evaluation.

Reproduce from `backend/` with project dev dependencies:

```bash
THREADLY_REQUIRE_TEST_DB=1 \
THREADLY_TEST_DB=postgresql+asyncpg://threadly:change-me@127.0.0.1:55451/threadly_test \
python -m pytest -q --tb=short
python -m ruff check app tests alembic/versions/c12026e9a032_calendar_reads.py
```

## Focused review and rollout

Reviewed identity/scope/ACL separation, preference ownership, input bounds,
account→preference lock order, token refresh fences, concurrent updates, expiry,
provider privacy, migration guards and old API compatibility. Normal OAuth login
still requests Gmail only; only authenticated reconnect can request named Calendar
read access. Provider errors cannot become free time or leak raw bodies. Declared the first-party
timezone-data fallback after reviewing minimal-host deployment dependencies; the
new isolated fallback test does not claim B13 DST/slot acceptance.

Apply the migration with matching API/workers after backup and normal deployment
sequencing. No new environment settings or AWS permissions. Configure the existing
OAuth client/callback and Google Calendar API before authorized account smoke tests.
Downgrade refuses with saved preferences/evidence. Evidence expiry invalidates reuse
but is not physical deletion; scheduled retention and abuse controls remain B19.
Exact-head CI result belongs in the PR after publication. Diagrams are Mermaid source,
not rendered acceptance evidence.

## Next work

B13 deterministic timezone/slot engine consumes owned, fresh complete evidence and
saved explicit preferences. Query with buffer padding around the desired slot
window; B12 allows 245 minutes of lookback so recently ended meetings can still
block near-term slots. Do not expand buffers only after discarding those events. B14/B15 then add negotiation and exact approved event
execution/reconciliation; Calendar multi-intent graphs remain unsupported meanwhile.
Live Calendar test owner must exercise partial consent, read-only/shared ACLs,
revocation, known busy vs empty windows and settings changed during reads.
T03/T15 evidence updated without declaring the broader packages verified.
